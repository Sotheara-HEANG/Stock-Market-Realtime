-- ============================================================
-- Gold Layer — Star Schema (Analytics / ML-ready)
-- Optimised for aggregations, dashboards, and ML feature tables.
-- ============================================================

CREATE SCHEMA IF NOT EXISTS gold;
